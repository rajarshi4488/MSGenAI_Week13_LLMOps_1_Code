---
name: SQL Generation
description: Generate SQL (DuckDB compatible) to answer the query.
authors:
  - User
model:
  api: chat
  configuration:
    type: azure_openai
    azure_deployment: gpt-5-mini
parameters:
  max_tokens: 1000
  temperature: 0.1
inputs:
  user_query:
    type: string
  schema_info:
    type: string
---
system:
You are an expert SQL analyst. Your goal is to write a SQL query to answer the user's question.
The table name is `supply_chain`.

Schema Information:
{{schema_info}}

Rules:
1. Return ONLY the SQL query. Do not use markdown like ```sql.
2. Use DuckDB compatible SQL.
3. The query must return the result directly.
4. Do not perform DML operations (INSERT, UPDATE, DELETE).

user:
{{user_query}}
